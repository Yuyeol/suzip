# Folder List

## 레이아웃

```
┌────────────────────────────────────────┐
│ 📁 개발 자료 (12)    [수정] [삭제]      │
└────────────────────────────────────────┘
┌────────────────────────────────────────┐
│ 📁 디자인 (5)        [수정] [삭제]      │
└────────────────────────────────────────┘
```

## Props

- `folders`: 폴더 배열 `{ id, name, itemCount }[]`
- `onEdit`: 수정 핸들러 `(id, name) => void`
- `onDelete`: 삭제 핸들러 `(id) => void`

## 기능

- 폴더 목록 렌더링
- 각 아이템: 폴더 정보 + 수정/삭제 버튼
- 빈 상태 처리

## 개별 아이템 스타일

- border border-border-light
- rounded-lg
- px-4 py-3
- flex justify-between items-center
- space-y-3 간격
