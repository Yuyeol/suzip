# Phase 7: 폴더 삭제 및 데이터 정합성 강화

폴더를 삭제할 때 소속된 북마크를 어떻게 처리할지에 대한 비즈니스 로직을 확립하고 구현합니다.

## 🎯 목표

- 폴더 삭제 시 사용자에게 명확한 선택권 제공
- **미분류(폴더 없음) 북마크**를 UI에서 쉽게 확인하고 관리할 수 있도록 개선
- 데이터 무결성 및 사용자 의도에 맞는 데이터 처리

## 📋 작업 목록

### 1. UI/UX 개선

- [x] **미분류 북마크 보기**: 홈 화면 또는 필터에 '미분류' 옵션을 추가하여 `folder_id`가 `null`인 북마크만 볼 수 있도록 구현
- [x] **폴더 삭제 확인 알럿**: 폴더 삭제 버튼 클릭 시, 소속 북마크 개수를 파악하여 "N개의 북마크가 저장되어 있습니다. 삭제하시겠습니까?" 메시지 표시
- [x] **탭 전환 시 필터 초기화**: (추가 작업) 상단 탭 '전체보기' 클릭 시 모든 필터가 초기화되도록 `ViewTabs` 로직 개선

### 2. API 및 백엔드 로직 수정

- [x] **북마크 조회 API 확장**: `folder_id=null` 파라미터를 처리하여 미분류 북마크만 필터링하는 기능 추가
- [x] **폴더 삭제 로직 확립**: 안전을 위해 하위 북마크 동시 삭제 기능은 구현하지 않고, '미분류 이동' 원칙을 알럿으로 안내하는 것으로 결정

### 3. 클라이언트 로직 수정

- [x] **폴더 내 북마크 카운트**: 삭제 전 해당 폴더의 북마크 개수를 API로 확인하는 로직 추가 (MoreButton에서 itemCount 활용)
- [x] **React Query Invalidation**: 폴더 삭제 후 관련 북마크 쿼리 무효화 (`useDeleteFolder`에서 `bookmarkKeys.all` 무효화 추가 완료)

## 💡 구현 방향성

### 미분류 북마크 필터링

- URL 쿼리 파라미터로 `folder_id=null`을 넘겼을 때, API에서 `.is('folder_id', null)` 필터를 적용하도록 구현합니다.
- 홈 화면의 필터 컨트롤(`FilterControls.tsx`)에 '미분류' 필터를 추가합니다.

### 폴더 삭제 알럿

- `useDeleteFolder` 훅 사용 전, 해당 폴더의 북마크 리스트를 가져오거나 개수를 조회하여 알럿을 띄웁니다.
- 사용자가 확인을 누르면 삭제 프로세스를 진행합니다.

**추천**: 알럿에서 "폴더만 삭제 (북마크는 유지)"와 "모두 삭제" 버튼을 따로 제공하거나, 기본은 유동적으로 처리하되 북마크가 있는 경우에만 경고를 주는 방식이 좋습니다.
